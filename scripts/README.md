# scripts

Helper scripts (build, deploy, db backup, etc.). Currently empty.

Planned additions:
- `dev.sh`        — start backend + bot + frontend in parallel
- `db-backup.sh`  — snapshot reyna.db before risky migrations
- `release.sh`    — build a single static binary for prod

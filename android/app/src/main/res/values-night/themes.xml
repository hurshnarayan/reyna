<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Same as values/themes.xml; the colours come from the night resources. -->
    <style name="Theme.Reyna" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
        <item name="android:windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_mark</item>
    </style>
</resources>

# configs

Configuration files. Currently empty — runtime config is loaded from `.env` at
the repo root via `internal/config`. Add YAML / TOML configs here if Reyna ever
grows beyond a single environment.
